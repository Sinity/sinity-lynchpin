apiVersion: 1
providers:
  - name: ActivityWatch Boards
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    allowUiUpdates: true
    options:
      path: /realm/session/activitywatch-grafana/dashboards
