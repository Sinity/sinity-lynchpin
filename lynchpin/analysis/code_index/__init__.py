"""Code-index analysis: language inventory, complexity, dependency graphs,
symbol indexes.

Modules under this namespace describe **project shape** — what code exists,
how it is structured, where centrality lives. They are read-only over the
active project set and feed into the change/interpretation layers.
"""
